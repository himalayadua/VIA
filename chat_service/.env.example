# NVIDIA NIM Configuration
NVIDIA_NIM_API_KEY=nvapi-your-api-key-here
NVIDIA_NIM_BASE_URL=https://integrate.api.nvidia.com/v1
NVIDIA_NIM_MODEL=meta/llama-3.1-70b-instruct

# Database Configuration (shared with Express.js)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=via_canvas
DB_USER=viacanvas
DB_PASSWORD=viacanvas_dev

# Server Configuration
PORT=8000
HOST=0.0.0.0

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Logging
LOG_LEVEL=INFO

# Qdrant Configuration (Vector Database for Knowledge Base)
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_COLLECTION=via_canvas_kb
QDRANT_API_KEY=  # Optional, for Qdrant Cloud

# RAG Configuration
RAG_CHUNK_SIZE=500  # Chunk size in words
RAG_CHUNK_OVERLAP=50  # Overlap in words
RAG_TOP_K=5  # Number of results to retrieve
RAG_SCORE_THRESHOLD=0.7  # Minimum similarity score
